import { UserType } from "../types/user-type";

type Props = {
    users: UserType[]
}

const Users = (props: Props) => {
    return (
        <div>
            
        </div>
    )
}

export default Users